export class User {
    constructor(emailID : string, password ?: string, userType? : string, address? : string, address2? : string,
                city? : string,state? : string, zipCode? : number) {

                }
}